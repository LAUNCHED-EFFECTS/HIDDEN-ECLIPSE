# demo

`globe.html` is a rendered scenario, checked in so the project can be looked at
without installing anything — open it in a browser.

It is generated output. To refresh it:

    python3 bin/globe.py --seed 1337 --no-open

The buttons on this page are inert; they need a process behind them. Run
`python3 bin/serve.py` for the interactive version.